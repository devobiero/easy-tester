import { afterEach, beforeEach, describe, expect, it } from '../src';

describe('Booleans', () => {
  beforeEach(() => {});

  it('should be truthy', () => {
    expect(true).toBeTruthy();
  });

  it('should be falsy', () => {
    expect(false).toBeFalsy();
  });

  afterEach(() => {});
});
